# ------------------------------------------------------------------------------
# GENERATED / ENFORCED BY STAGEFREIGHT GOVERNANCE
# Source repo: PrPlanIT/MaintenancePolicy
# Source ref: b031763542a156f404162c2cf5ce4e4dc1e9993b
# Profile: rebaseable-fork-docker
# This .stagefreight.yml is governed, not purely local.
# To disable governance, detach from the control repo workflow.
# Manual changes may be overwritten by reconciliation.
# ------------------------------------------------------------------------------

version: 1
vars:
  github_org: PrPlanIT
  github_repo: prometheus-eaton-ups-exporter
  gitlab_group: HomeLabHD
  license: MIT
  org: prplanit
  repo: prometheus-eaton-ups-exporter
builds:
  preset: preset/build-docker.yml
scribe:
  preset: preset/scribe-docker.yml
lint:
  preset: preset/lint-generic.yml
security:
  preset: preset/security.yml
dependency:
  preset: preset/dependency.yml
commit:
  preset: preset/commit.yml
release:
  preset: preset/release.yml
manifest:
  preset: preset/manifest.yml
build_cache:
  preset: preset/build_cache.yml
ci:
  image: docker.cr.pcfae.com/prplanit/stagefreight:latest-dev
forges:
  preset: preset/forges.yml
git:
  preset: preset/git.yml
metadata:
  category: monitoring
  description:
    - Prometheus exporter for Eaton UPS devices via the Eaton REST API.
    - Rootless container exporting Eaton UPS load, runtime, battery, and I/O metrics to Prometheus, scraped over the UPS web API.
  license: MIT
  org: HomeLabHD
  title: Eaton UPS Exporter
  topics:
    - prometheus
    - exporter
    - ups
    - eaton
    - monitoring
orgs:
  HomeLabHD:
    aliases:
      docker: hlhd
      ghcr: homelabhd
      github: HomeLabHD
      handle: hlhd
    maintainer: HomeLabHD <homelabhelp@gmail.com>
preset_source:
  cache_policy: authoritative
  project_id: PrPlanIT/MaintenancePolicy
  provider: gitlab
  ref: b031763542a156f404162c2cf5ce4e4dc1e9993b
  repo_url: https://gitlab.prplanit.com
publish:
  preset: preset/publish-docker.yml
registries:
  preset: preset/registries.yml
repos:
  github-mirror:
    forge: github
    project: HomeLabHD/prometheus-eaton-ups-exporter
    roles:
      - mirror
      - publish-origin
    sync:
      git: true
      releases: true
  primary:
    branches:
      default: main
    forge: gitlab
    project: HomeLabHD/prometheus-eaton-ups-exporter
    roles:
      - primary
    worktree: .
stencils:
  presets:
    - preset/stencils-base.yml
    - preset/stencils-status.yml
    - preset/stencils-docker.yml
test:
  preset: preset/test-python.yml
toolchains:
  preset: preset/toolchains.yml
